1. Query Python environment and package versions:
   python -c "import sys, sklearn, scipy, numpy, pandas, cv2; print(sys.version); print('sklearn:', sklearn.__version__); print('scipy:', scipy.__version__); print('numpy:', numpy.__version__); print('pandas:', pandas.__version__); print('opencv:', cv2.__version__)"

2. Run standard deterministic EER verification at k=128:
   python _paper_resolution/run_test.py

3. Re-run legacy Gabor+PCA and Gabor+XPCA comparison script:
   python _paper_resolution/reproduce_run_gabor_xpca.py

4. Run 30 randomized split paired trials, save splits, raw scores, logs, and compute statistical tests (t-test, Wilcoxon, permutation):
   python _paper_resolution/run_everything.py

5. Compile the final authoritative benchmark table:
   python _paper_resolution/create_authoritative_benchmark.py
